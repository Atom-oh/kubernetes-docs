apiVersion: v1
kind: Pod
metadata:
  name: case
  namespace: policy-lab
  labels:
    app.kubernetes.io/name: demo
spec:
  containers:
  - name: app
    image: nginx:latest
    resources:
      limits:
        cpu: 500m
        memory: 128Mi
