apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: policy-lab
  name: other
spec:
  rules:
  - host: app.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: demo
            port:
              number: 80
