apiVersion: v1
kind: Pod
metadata:
  name: case
  namespace: policy-lab
spec:
  containers:
  - name: app
    image: registry.example.com/team/app:v1
    resources:
      limits:
        cpu: 500m
        memory: 128Mi
