apiVersion: constraints.gatekeeper.sh/v1beta1
kind: DocsUniqueIngress
metadata:
  name: unique-ingress
spec:
  enforcementAction: dryrun
  match:
    kinds:
    - apiGroups:
      - networking.k8s.io
      kinds:
      - Ingress
    namespaces:
    - policy-lab
